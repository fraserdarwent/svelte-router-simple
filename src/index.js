// const svelte = require('svelte/compiler');

// const router = svelte.compile('./components/Router/Router').js.code;

// console.log(router)

// This file is used to export the objects

import Router from './components/Router/Router'

export default Router